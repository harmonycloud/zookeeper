kind: ConfigMap
metadata:
  name: {{ include "zookeeper-operator.fullname" . }}-dashboard
  labels:
    grafana_dashboard: "1" 
apiVersion: v1
data:
{{ (.Files.Glob "dashboard/*").AsConfig | indent 2 }}